#include <stdio.h>
#include<string.h>

int main(){
    char str[100];
    int word=0;
    printf("Enter string: ");
    gets(str);
    char comp[strlen(str)];

    for(int i=0 ;i<strlen(str) ;i++)
    {
                            
    }

    printf("Number of words in String is %d",word);
        
    return 0;
}